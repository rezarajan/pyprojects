# Poisson Python Library

A small library for Poisson distribution utilities. Project managed using the uv tool.

# Testing

```sh
# Install deps
uv sync
```

```sh
# Run the pytest test
uv run pytest
```
